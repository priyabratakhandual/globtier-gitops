apiVersion: v1
kind: Service
metadata:
  name: {{ include "globtier-app.fullname" . }}
  labels:
    {{- include "globtier-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "globtier-app.selectorLabels" . | nindent 4 }}
